apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: monorepo-service-50
  description: "Monorepo service 50 — scale test (TC-037)"
  tags:
    - monorepo
    - service-50
spec:
  type: service
  lifecycle: experimental
  owner: user:default/test-user
