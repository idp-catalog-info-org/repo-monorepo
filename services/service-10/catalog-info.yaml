apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: monorepo-service-10
  description: "Monorepo service 10 — scale test (TC-037)"
  tags:
    - monorepo
    - service-10
spec:
  type: service
  lifecycle: experimental
  owner: user:default/test-user
