# Monorepo Scale Test Repository

Contains 50 service directories each with a `catalog-info.yaml` for **TC-037** (bulk import scale test).

Use catalog path: `**/catalog-info.yaml`

All 50 entities should be discovered and registered without truncation.
